<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Klinikum Chronicles</title>
<style>
@import url('https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap');

:root{
  --bg-deep:#0b1622;
  --bg-panel:#12233a;
  --bg-panel-light:#1a3355;
  --border-glow:#5ad1e6;
  --text-main:#eaf6fb;
  --text-dim:#8fb0c4;
  --hp-color:#ff5d73;
  --mp-color:#5ad1e6;
  --xp-color:#ffd166;
  --heal-color:#59d98c;
  --gold:#ffd166;
  --danger:#ff5d73;
  --olivia:#7c9dff;
  --lisa:#59d98c;
  --falco:#ff9f6b;
  --daniel:#c58bff;
  --tobi:#ffcf56;
  --pixel: 'Press Start 2P', monospace;
  --body: 'VT323', monospace;
}
*{box-sizing:border-box;}
html,body{margin:0;padding:0;height:100%;background:var(--bg-deep);}
body{
  font-family:var(--body);
  color:var(--text-main);
  font-size:20px;
  display:flex;
  justify-content:center;
  align-items:center;
  min-height:100vh;
  background:
    radial-gradient(circle at 20% 10%, #132a44 0%, transparent 40%),
    radial-gradient(circle at 80% 90%, #0f2036 0%, transparent 40%),
    var(--bg-deep);
}
#game{
  width:100%;
  max-width:900px;
  height:640px;
  max-height:96vh;
  background:var(--bg-panel);
  border:4px solid var(--border-glow);
  border-radius:10px;
  box-shadow:0 0 0 4px #06111c, 0 0 30px rgba(90,209,230,0.35), inset 0 0 40px rgba(0,0,0,0.4);
  position:relative;
  overflow:hidden;
  display:flex;
  flex-direction:column;
}
.hidden{display:none !important;}
.pixel{font-family:var(--pixel);}
h1,h2,h3{font-family:var(--pixel); margin:0; letter-spacing:1px;}
button{
  font-family:var(--body);
  font-size:20px;
  cursor:pointer;
  background:var(--bg-panel-light);
  color:var(--text-main);
  border:2px solid var(--border-glow);
  border-radius:4px;
  padding:8px 14px;
  transition:transform .08s, background .15s;
}
button:hover:not(:disabled){background:#254468; transform:translateY(-1px);}
button:active:not(:disabled){transform:translateY(1px);}
button:disabled{opacity:.35; cursor:not-allowed;}
.scanlines::after{
  content:"";
  position:absolute; inset:0;
  background:repeating-linear-gradient(rgba(0,0,0,0) 0, rgba(0,0,0,0) 2px, rgba(0,0,0,0.12) 3px);
  pointer-events:none;
  mix-blend-mode:multiply;
}

/* ---------- TITLE ---------- */
#screen-title{
  flex:1; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:28px;
  background:
    linear-gradient(180deg, rgba(11,22,34,.2), rgba(11,22,34,.9)),
    repeating-linear-gradient(90deg, #0e1e30 0 40px, #0f2136 40px 80px);
  text-align:center;
}
#screen-title h1{font-size:32px; color:var(--border-glow); text-shadow:0 0 10px rgba(90,209,230,.7); line-height:1.6;}
#screen-title .sub{font-size:22px; color:var(--text-dim); font-family:var(--body);}
#screen-title .cross{font-size:54px; color:var(--hp-color); text-shadow:0 0 14px rgba(255,93,115,.8);}
.title-btns{display:flex; flex-direction:column; gap:10px;}
.title-btns button{width:260px; font-size:18px;}

/* ---------- TOP BAR ---------- */
#topbar{
  display:flex; align-items:center; justify-content:space-between;
  padding:8px 14px; background:#0d1c2d; border-bottom:2px solid var(--border-glow);
  font-family:var(--pixel); font-size:11px; color:var(--gold);
}
#topbar .floorlabel{color:var(--text-main);}

/* ---------- MAP / ELEVATOR ---------- */
#screen-map{flex:1; display:flex; overflow:hidden;}
#elevator-panel{
  width:230px; background:#0d1c2d; border-right:2px solid var(--border-glow);
  padding:14px 10px; display:flex; flex-direction:column; gap:6px; overflow-y:auto;
}
#elevator-panel h3{font-size:11px; color:var(--border-glow); margin-bottom:8px;}
.floor-btn{
  display:flex; justify-content:space-between; align-items:center;
  font-family:var(--body); font-size:18px; padding:8px 10px;
  border:2px solid #2a4a6b; border-radius:4px; background:#132a44;
}
.floor-btn.locked{opacity:.35;}
.floor-btn.done{border-color:var(--heal-color); color:var(--heal-color);}
.floor-btn.current{border-color:var(--gold); box-shadow:0 0 8px rgba(255,209,102,.5); color:var(--gold);}
.floor-btn button{padding:4px 10px; font-size:14px;}
#map-scene{
  flex:1; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:18px;
  padding:20px; text-align:center;
  background:radial-gradient(circle at 50% 30%, #16304d 0%, #0b1826 75%);
}
#map-scene .dept-icon{font-size:70px; filter:drop-shadow(0 0 12px rgba(90,209,230,.5));}
#map-scene h2{color:var(--text-main); font-size:16px;}
#map-scene p{color:var(--text-dim); max-width:420px; font-size:19px;}
.map-actions{display:flex; gap:10px; flex-wrap:wrap; justify-content:center;}

/* ---------- BOTTOM NAV (party menu etc) ---------- */
#bottomnav{
  display:flex; gap:8px; padding:8px 14px; background:#0d1c2d; border-top:2px solid var(--border-glow);
  justify-content:center; flex-wrap:wrap;
}
#bottomnav button{font-size:14px; padding:6px 10px;}

/* ---------- DIALOGUE ---------- */
#screen-dialogue{
  position:absolute; left:0; right:0; bottom:0; top:0;
  display:flex; flex-direction:column; justify-content:flex-end;
  background:rgba(6,12,20,.35);
}
#dlg-box{
  margin:0 14px 14px 14px; background:#0d1c2d; border:3px solid var(--border-glow); border-radius:8px;
  padding:16px 18px; min-height:130px; display:flex; flex-direction:column; gap:8px;
  box-shadow:0 0 20px rgba(0,0,0,.6);
}
#dlg-speaker{font-family:var(--pixel); font-size:13px;}
#dlg-text{font-size:22px; line-height:1.4; flex:1;}
#dlg-next{align-self:flex-end; font-size:14px;}

/* ---------- BATTLE ---------- */
#screen-battle{flex:1; display:flex; flex-direction:column;}
#battle-stage{
  flex:1; position:relative; display:flex; align-items:flex-end; justify-content:center; gap:26px; padding:20px 10px 10px;
  background:
    linear-gradient(180deg, #1c3a5c 0%, #142944 55%, #0d1c2d 100%);
  overflow:hidden;
}
#battle-stage::before{
  content:""; position:absolute; inset:0;
  background:repeating-linear-gradient(90deg, rgba(255,255,255,.02) 0 60px, transparent 60px 120px);
}
.enemy-sprite{
  position:relative; display:flex; flex-direction:column; align-items:center; gap:6px; cursor:pointer;
  transition:transform .15s;
}
.enemy-sprite:hover{transform:translateY(-4px);}
.enemy-sprite.dead{opacity:.15; pointer-events:none; filter:grayscale(1);}
.enemy-sprite .emoji{font-size:64px; filter:drop-shadow(0 6px 6px rgba(0,0,0,.5));}
.enemy-sprite.boss .emoji{font-size:96px;}
.enemy-sprite .ename{font-family:var(--pixel); font-size:9px; color:var(--text-main); text-align:center; max-width:110px;}
.ehpbar{width:90px; height:8px; background:#06111c; border:1px solid #274a6b; border-radius:3px; overflow:hidden;}
.ehpbar > div{height:100%; background:var(--hp-color);}
.enemy-sprite.targetable .emoji{animation:bob 1.2s infinite ease-in-out;}
@keyframes bob{0%,100%{transform:translateY(0);}50%{transform:translateY(-6px);}}
#floatnums{position:absolute; inset:0; pointer-events:none;}
.floatnum{position:absolute; font-family:var(--pixel); font-size:14px; color:var(--hp-color); text-shadow:0 0 4px #000; animation:rise 1s forwards;}
.floatnum.heal{color:var(--heal-color);}
@keyframes rise{0%{opacity:1; transform:translate(-50%,0);}100%{opacity:0; transform:translate(-50%,-40px);}}

#party-row{display:flex; gap:8px; padding:8px 10px; background:#0d1c2d; border-top:2px solid var(--border-glow); flex-wrap:wrap;}
.pcard{
  flex:1; min-width:150px; background:#132a44; border:2px solid #2a4a6b; border-radius:6px; padding:8px 10px;
  cursor:pointer; position:relative;
}
.pcard.active-turn{border-color:var(--gold); box-shadow:0 0 10px rgba(255,209,102,.5);}
.pcard.dead{opacity:.35;}
.pcard.target-select:hover{border-color:var(--heal-color);}
.pcard .pname{font-family:var(--pixel); font-size:10px; margin-bottom:4px;}
.bar{height:9px; border-radius:3px; background:#06111c; border:1px solid #274a6b; overflow:hidden; margin:3px 0;}
.bar > div{height:100%;}
.bar.hp > div{background:var(--hp-color);}
.bar.mp > div{background:var(--mp-color);}
.barlabel{font-size:13px; color:var(--text-dim); display:flex; justify-content:space-between;}

#battle-menu{display:flex; gap:8px; padding:10px 14px; background:#0d1c2d; flex-wrap:wrap; align-items:flex-start; min-height:60px;}
#battle-log{
  font-size:17px; color:var(--text-dim); padding:0 14px 8px; min-height:22px;
}
.menu-col{display:flex; flex-direction:column; gap:6px;}
.skill-btn{font-size:14px; text-align:left; max-width:260px;}
.skill-btn small{display:block; color:var(--text-dim); font-size:12px;}

/* ---------- PARTY / SKILLTREE ---------- */
#screen-party{flex:1; display:flex; flex-direction:column; overflow:hidden;}
#party-tabs{display:flex; gap:6px; padding:10px 14px; background:#0d1c2d; border-bottom:2px solid var(--border-glow); flex-wrap:wrap;}
.tab-btn.selected{border-color:var(--gold); color:var(--gold);}
#party-detail{flex:1; overflow-y:auto; padding:16px 20px;}
.stat-grid{display:grid; grid-template-columns:1fr 1fr; gap:6px 20px; margin:10px 0 16px;}
.stat-grid div{font-size:18px; color:var(--text-dim);}
.stat-grid b{color:var(--text-main);}

.tree-wrap{display:flex; flex-direction:column; gap:22px; align-items:flex-start;}
.tree-tier{display:flex; gap:18px; flex-wrap:wrap;}
.node{
  width:210px; border:2px solid #2a4a6b; background:#132a44; border-radius:8px; padding:10px 12px; position:relative;
}
.node.unlocked{border-color:var(--heal-color); box-shadow:0 0 8px rgba(89,217,140,.4);}
.node.available{border-color:var(--gold);}
.node.locked{opacity:.5;}
.node .nname{font-family:var(--pixel); font-size:11px; margin-bottom:6px;}
.node .ndesc{font-size:15px; color:var(--text-dim); margin-bottom:8px; min-height:44px;}
.node button{width:100%; font-size:13px;}

/* ---------- GAME OVER / ENDING ---------- */
#screen-gameover, #screen-ending{
  flex:1; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:16px; text-align:center; padding:20px;
}
#screen-gameover h1{color:var(--hp-color); font-size:26px;}
#screen-ending h1{color:var(--gold); font-size:24px; line-height:1.6;}
#screen-ending p{max-width:520px; color:var(--text-dim); font-size:20px;}

::-webkit-scrollbar{width:8px;}
::-webkit-scrollbar-thumb{background:#2a4a6b; border-radius:4px;}
</style>
</head>
<body>
<div id="game" class="scanlines">

  <!-- TITLE -->
  <div id="screen-title">
    <div class="cross">✚</div>
    <h1>KLINIKUM<br>CHRONICLES</h1>
    <div class="sub">Ein JRPG-Prototyp aus dem Krankenhausalltag</div>
    <div class="title-btns">
      <button onclick="Game.newGame()">Neues Spiel starten</button>
      <button onclick="Game.showHelp()">Wie spiele ich?</button>
    </div>
  </div>

  <!-- HELP -->
  <div id="screen-help" class="hidden" style="flex:1; padding:24px; overflow-y:auto;">
    <h2 style="color:var(--border-glow); font-size:16px; margin-bottom:14px;">Anleitung</h2>
    <p style="font-size:19px; line-height:1.5; color:var(--text-dim);">
    Olivia, Neurologin im Klinikum, kämpft sich durch 10 Etagen voller Krankheiten,
    nerviger Angehöriger und anstrengender Kollegen. Unterwegs schließen sich ihr
    Lisa (Internistin), Falco (Gynäkologe), Daniel (Anästhesist) und Tobi (Unfallchirurg) an.<br><br>
    • Im Aufzug-Menü wählst du die nächste Etage.<br>
    • In Kämpfen wählt ihr rundenbasiert Angriff, Fähigkeit, Verteidigen oder Item.<br>
    • Nach jedem Sieg gibt es EP und ab und zu einen Skillpunkt.<br>
    • Im Party-Menü investierst du Skillpunkte in den Skilltree jedes Charakters,
    um neue Fähigkeiten freizuschalten.<br><br>
    Viel Erfolg auf der Visite!
    </p>
    <button style="margin-top:16px;" onclick="Game.backToTitle()">Zurück</button>
  </div>

  <!-- TOPBAR (shown in map/party) -->
  <div id="topbar" class="hidden">
    <div class="floorlabel">Etage <span id="tb-floor">1</span>/10</div>
    <div id="tb-gold" class="pixel"></div>
  </div>

  <!-- MAP -->
  <div id="screen-map" class="hidden">
    <div id="elevator-panel">
      <h3>🛗 AUFZUG</h3>
      <div id="floor-list"></div>
    </div>
    <div id="map-scene"></div>
  </div>
  <div id="bottomnav" class="hidden">
    <button onclick="Game.openParty()">👥 Party & Skilltree</button>
    <button onclick="Game.backToMap()">🛗 Aufzug</button>
  </div>

  <!-- DIALOGUE -->
  <div id="screen-dialogue" class="hidden">
    <div id="dlg-box">
      <div id="dlg-speaker" class="pixel"></div>
      <div id="dlg-text"></div>
      <button id="dlg-next" onclick="Game.advanceDialogue()">weiter ▶</button>
    </div>
  </div>

  <!-- BATTLE -->
  <div id="screen-battle" class="hidden">
    <div id="battle-stage">
      <div id="enemy-list" style="display:flex; gap:26px;"></div>
      <div id="floatnums"></div>
    </div>
    <div id="battle-log"></div>
    <div id="party-row"></div>
    <div id="battle-menu"></div>
  </div>

  <!-- PARTY / SKILLTREE -->
  <div id="screen-party" class="hidden">
    <div id="party-tabs"></div>
    <div id="party-detail"></div>
  </div>

  <!-- GAME OVER -->